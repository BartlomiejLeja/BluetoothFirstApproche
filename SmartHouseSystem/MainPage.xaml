<Page
    x:Class="SmartHouseSystem.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartHouseSystem"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <Button Name="btnConnect"
          Content="Connect"
          Click="btnConnect_Click"
          FontSize="40" />
            <Button Name="btnDisconnect"
          Content="Disconnect"
          Click="btnDisconnect_Click"
          FontSize="40" />
            <TextBox Name="tbInput"
           Text="2"
           FontSize="40" />
            <Button Name="btnSend"
          Content="Send"
          Click="btnSend_Click"
          FontSize="40" />
            <TextBlock Name="tbError"
             FontSize="40" />
        </StackPanel>
        <Image x:Name="videoImage" HorizontalAlignment="Left" Height="202" Margin="0,317,0,0" VerticalAlignment="Top" Width="360"/>
        <Button x:Name="startButton" Content="Button" HorizontalAlignment="Left" Margin="119,568,0,0" VerticalAlignment="Top" Width="100" Click="startButton_Click"/>
    </Grid>
</Page>
